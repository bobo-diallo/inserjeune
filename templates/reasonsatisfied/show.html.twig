{% extends 'reasonsatisfied/content.html.twig' %}

{% block body %}
    <div class="content-i">
        <div class="content-box">
          {# Tableau 1 #}
            <div class="element-wrapper">
                <h6 class="element-header">
                    Details
                    <a class="btn btn-sm btn-primary float-right" href="{{ path('reasonsatisfied_new') }}">
                        <i class="fa fa-plus"></i> Ajouter une raison
                    </a>
                    <a class="btn btn-sm btn-primary float-right kz_back" href="{{ path('reasonsatisfied_index') }}">
                        <i class="fa fa-arrow-left"></i> Retour à la liste
                    </a>
                </h6>
                <div class="element-box">
                    <form>
                        <div class="form-group row">
                            <label class="text-uppercase col-form-label col-sm-4">Id</label>
                            <div class="col-sm-8"><input class="form-control" disabled value="{{ reasonSatisfied.id }}"></div>
                        </div>
                        <div class="form-group row">
                            <label class="text-uppercase col-form-label col-sm-4">Name</label>
                            <div class="col-sm-8"><input class="form-control" disabled value="{{ reasonSatisfied.name }}"></div>
                        </div>
                    </form>
                    <div class="controls-above-table">
                        <div class="row" id="actions">
                            <div class="col-sm-6">
                                <a class="btn btn-sm btn-primary" href="{{ path('reasonsatisfied_edit', {'id': reasonSatisfied.id}) }}">
                                    <i class="fa fa-edit" aria-hidden="true"></i> Modifier</a>
                              {{ form_start(delete_form) }}
                                <button class="btn btn-sm btn-danger" type="sumbit">
                                    <i class="os-icon os-icon-database-remove" aria-hidden="true"></i>Supprimer</button>
                              {{ form_end(delete_form) }}

                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}
