{#{% extends 'base.html.twig' %}#}

{#{% block body %}#}
    {#<h1>Usefultrainingreason</h1>#}

    {#<table>#}
        {#<tbody>#}
            {#<tr>#}
                {#<th>Id</th>#}
                {#<td>{{ usefulTrainingReason.id }}</td>#}
            {#</tr>#}
            {#<tr>#}
                {#<th>Reason</th>#}
                {#<td>{{ usefulTrainingReason.reason }}</td>#}
            {#</tr>#}
        {#</tbody>#}
    {#</table>#}

    {#<ul>#}
        {#<li>#}
            {#<a href="{{ path('usefultrainingreason_index') }}">Back to the list</a>#}
        {#</li>#}
        {#<li>#}
            {#<a href="{{ path('usefultrainingreason_edit', { 'id': usefulTrainingReason.id }) }}">Edit</a>#}
        {#</li>#}
        {#<li>#}
            {#{{ form_start(delete_form) }}#}
                {#<input type="submit" value="Delete">#}
            {#{{ form_end(delete_form) }}#}
        {#</li>#}
    {#</ul>#}
{#{% endblock %}#}
{% extends 'usefultrainingreason/content.html.twig' %}

{% block body %}
    <div class="content-i">
        <div class="content-box">
          {# Tableau 1 #}
            <div class="element-wrapper">
                <h6 class="element-header">
                    {{ 'menu.detail' | trans }}
                    <a class="btn btn-sm btn-primary float-right" href="{{ path('usefultrainingreason_new') }}">
                        <i class="fa fa-plus"></i> {{ 'menu.add_a_reason' | trans }}
                    </a>
                    <a class="btn btn-sm btn-primary float-right kz_back" href="{{ path('usefultrainingreason_index') }}">
                        <i class="fa fa-arrow-left"></i> {{ 'menu.back_to_the_list' | trans }}
                    </a>
                </h6>
                <div class="element-box">
                    <form>
                        <div class="form-group row">
                            <label class="text-uppercase col-form-label col-sm-4">Id</label>
                            <div class="col-sm-8"><input class="form-control" disabled value="{{ usefulTrainingReason.id }}"></div>
                        </div>
                        <div class="form-group row">
                            <label class="text-uppercase col-form-label col-sm-4">Name</label>
                            <div class="col-sm-8"><input class="form-control" disabled value="{{ usefulTrainingReason.name }}"></div>
                        </div>
                    </form>
                    <div class="controls-above-table">
                        <div class="row" id="actions">
                            <div class="col-sm-6">
                                <a class="btn btn-sm btn-primary" href="{{ path('usefultrainingreason_edit', {'id': usefulTrainingReason.id}) }}">
                                    <i class="fa fa-edit" aria-hidden="true"></i> {{ 'menu.edit' | trans }}</a>
                              {{ form_start(delete_form) }}
                                <button class="btn btn-sm btn-danger" type="sumbit">
                                    <i class="os-icon os-icon-database-remove" aria-hidden="true"></i>{{ 'menu.delete' | trans }}</button>
                              {{ form_end(delete_form) }}

                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}
