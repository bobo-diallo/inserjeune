{% extends 'omissionreason/content.html.twig' %}

{% block body %}
    <div class="content-i">
        <div class="content-box">
          {# Tableau 1 #}
            <div class="element-wrapper">
                <h6 class="element-header">
                    Lacunes des employés
                  {#{% include '_menu.html.twig' %}#}
                </h6>
                <div class="element-box">
                    <div class="controls-above-table">
                        <div class="row">
                            <div class="col-sm-6">
                                <a class="btn btn-sm btn-primary" href="#">Export CSV</a>
                                <a class="btn btn-sm btn-primary" href="#">Archiver</a>
                                <a class="btn btn-sm btn-primary" href="{{ path('omissionreason_new') }}">
                                    <i class="fa fa-plus"></i> ajouter une raison</a>
                            </div>
                        </div>
                    </div>
                    <div class="table-responsive">
                        <table id="kz_table" class="table table-bordered dt-responsive nowrap" style="width:100%">
                            <thead>
                            <tr>
                                <th >Actions</th>
                                <th><div><span>Id</span></div></th>
                                <th><div><span>Raison</span></div></th>
                            </tr>
                            </thead>
                            <tbody>
                            {% for reason in omissionReasons %}
                                <tr>
                                    <td class="row-actions">
                                        <a href="{{ path('omissionreason_edit', { 'id': reason.id }) }}#"><img src="{{ asset('build/images/icon/edit_16.png') }}" alt="edit"></a>
                                        <a href="{{ path('omissionreason_show', { 'id': reason.id }) }}"><img src="{{ asset('build/images/icon/show_16.png') }}" alt="show"></a>
                                        <a class="danger" href="#"><img src="{{ asset('build/images/icon/delete_16.png') }}" alt="delete"></a>
                                    </td>
                                    <td><a href="{{ path('omissionreason_show', { 'id': reason.id }) }}">{{ reason.id }}</a></td>
                                    <td>{{ reason.name }}</td>
                                </tr>
                            {% endfor %}
                            </tbody>
                        </table>
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}


{% block js %}
    <script>
       $(document).ready(function () {
          let table = datatable()
       });
       makeSelected('reason')
    </script>
{% endblock %}
