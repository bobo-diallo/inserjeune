{% extends 'school/content.html.twig' %}

{% block body %}
    <div class="content-i">
        <div class="content-box">
          {# Tableau 1 #}
            <div class="element-wrapper">
                <h6 class="element-header">
                    Liste des établissements
                  {#{% include '_menu.html.twig' %}#}
                </h6>
                <div class="element-box">
                    {%  if app.user.roles|role =="Administrateur" %}
                        <div class="controls-above-table">
                            <div class="row">
                                <div class="col-sm-6">
{#                                    <a class="btn btn-sm btn-primary" href="#">Archiver</a>#}
                                    {#<a class="btn btn-sm btn-primary" href="{{ path('school_new') }}">#}
                                        {#<i class="fa fa-plus"></i> Ajouter une école</a>#}
                                </div>
                            </div>
                        </div>
                    {% endif %}
                    <div class="table-responsive">
                        <table id="kz_table" class="table table-striped table-bordered dt-responsive nowrap" style="width:100%">
                            <thead>
                            <tr>
                                <th class="no-print">Actions</th>
                                {%  if app.user.roles|role =="Administrateur" %}
                                    <th>Id</th>
                                {% endif %}
                                <th>Immat.</th>
                                <th>Nom</th>
                                <th>Ville</th>
                                {#<th>Adresse numéro</th>#}
                                {#<th>Adresse rue</th>#}
                                {#<th>Adresse localité</th>#}
                                <th>Téléphone</th>
                                <th>Email</th>
                                {%  if app.user.roles|role =="Législateur" %}
                                    <th>Nombre de<br>diplômés</th>
                                {% endif %}
                                {#<th>Latitude</th>#}
                                {#<th>Longitude</th>#}
                                <th>date de<br>création</th>
                            </tr>
                            </thead>
                            <tbody>
                            {% for school in schools %}
                                <tr>
                                    <td class="row-actions">
                                        {%  if app.user.roles|role =="Administrateur" %}
                                            <a href="{{ path('school_edit', { 'id': school.id }) }}#"><img src="{{ asset('build/images/icon/edit_16.png') }}" alt="edit"></a>
                                        {% endif %}
                                        <a href="{{ path('school_show', { 'id': school.id }) }}"><img src="{{ asset('build/images/icon/show_16.png') }}" alt="show"></a>
                                        <a class="danger" onclick="deleteElement('{{ path('school_delete', {'id': school.id}) }}')"><img src="{{ asset('build/images/icon/delete_16.png') }}" alt="delete"></a>
                                    </td>
                                    {%  if app.user.roles|role =="Administrateur" %}
                                        <td><a href="{{ path('school_show', { 'id': school.id }) }}">{{ school.id }}</a></td>
                                    {% endif %}
                                    <td>{{ school.registration }}</td>
                                    <td>{{ school.name }}</td>
                                    {%  if app.user.roles|role =="Administrateur" %}
                                        <td>{{ school.city }}</td>
                                    {%  elseif app.user.roles|role =="Législateur" %}
                                        <td>{{ school.region.name }}, {{ school.city.name }}</td>
                                    {% endif %}
                                    {#<td>{{ school.addressNumber }}</td>#}
                                    {#<td>{{ school.addressLocality }}</td>#}
                                    {#<td>{{ school.addressRoad }}</td>#}
                                    <td>{{ school.phoneStandard }}</td>
                                    <td>{{ school.email }}</td>
                                    {%  if app.user.roles|role =="Législateur" %}
                                      <td>{{ school.personDegrees | length }}</td>
                                    {% endif %}
                                    {#<td>{{ school.latitude }}</td>#}
                                    {#<td>{{ school.longitude }}</td>#}
                                    <td>{{ school.createdDate|date('d M Y H:i') }}</td>
                                </tr>
                            {% endfor %}
                            </tbody>
                        </table>
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}


{% block js %}
    <script>
       $(document).ready(function () {
          let table = datatable(true)
       });
       makeSelected('school')
    </script>
{% endblock %}
