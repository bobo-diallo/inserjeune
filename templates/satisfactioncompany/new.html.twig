{% extends 'satisfactioncompany/content.html.twig' %}


{% block body %}
  <div class="content-i">
    <div class="content-box">
      {# Tableau 1 #}
      <div class="element-wrapper">
        <img src="{{ asset('build/images/icon/satisfaction.png') }}" alt="locality" class="title-icon"
             style="margin-bottom: -35px;">
        <h6 class="element-header">
          Nouvelle enquête de satisfaction 
          <a class="btn btn-sm btn-primary float-right" href="{{ path('satisfactioncompany_index' | route_company) }}">
            <i class="fa fa-arrow-left"></i> Retour à la liste
          </a>
        </h6>
        <div class="element-box">
          <form id="" method="post" action="{{ path('satisfactioncompany_new' | route_company) }}">
            {% include 'satisfactioncompany/_new.html.twig' %}
          </form>
        </div>
      </div>
    </div>
  </div>
{% endblock %}

