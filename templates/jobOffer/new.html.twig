{% extends 'jobOffer/content.html.twig' %}
{% block body %}
    <div class="content-i">
        <div class="content-box">
          {# Tableau 1 #}
            <div class="element-wrapper">
                <img src="{{ asset('build/images/icon/job.png') }}" alt="job" class="title-icon" style="margin-bottom: -35px;">
                <h6 class="element-header">
                    {{ 'job.adding_a_job_offer' | trans }}
                    <a class="btn btn-sm btn-primary float-right" href="{{ path('jobOffer_index') }}">
                        <i class="fa fa-arrow-left"></i> {{ 'menu.back_to_the_list' | trans }}
                    </a>
                </h6>
                <div class="element-box">
                    <form id="" method="post" enctype="multipart/form-data" action="{{ path('jobOffer_new') }}">
                      {% include 'jobOffer/_new.html.twig' %}
                    </form>
                </div>
            </div>
        </div>
    </div>
{% endblock %}



